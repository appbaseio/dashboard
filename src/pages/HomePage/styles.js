import { css } from 'emotion';

const headingText = css`
	margin-bottom: 0;
	margin-top: 0.5em;
	justify-content: center;
	font-weight: 500;
`;

const statsText = css`
	margin-bottom: 0;
	font-weight: 300;
`;

export { statsText, headingText };
